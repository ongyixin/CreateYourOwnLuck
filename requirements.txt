# System prerequisites
# This is a Node.js/Next.js project. npm dependencies are managed via package.json.
# Run `npm install` after cloning — no Python or pip required.

# ── Runtime ─────────────────────────────────────────────────────────────────
node >= 20.0.0      # Tested on v22.19.0; Dockerfile uses node:20-alpine
npm  >= 10.0.0      # Tested on v11.7.0

# ── External services (API keys required) ───────────────────────────────────
# Copy .env.example → .env.local and fill in at least one of each group:

# Web scraping (required)
apify             # https://apify.com — free tier available

# AI provider (pick one)
anthropic         # https://console.anthropic.com  — claude-sonnet-4-6
openai            # https://platform.openai.com    — gpt-4o
google-gemini     # https://aistudio.google.com    — gemini-2.5-pro

# ── Key npm dependencies (pinned versions from package.json) ─────────────────
next@^14.2.35
react@^18
typescript@^5
ai@^6.0.134                  # Vercel AI SDK
@ai-sdk/anthropic@^1.0.0
@ai-sdk/openai@^1.0.0
@ai-sdk/google@^3.0.52
apify-client@^2.9.0
zod@^3.22.0
tailwindcss@^3.4.17
framer-motion@^12.38.0
recharts@^3.8.0
@react-pdf/renderer@^4.3.2  # PDF export
unpdf@^1.4.0                # PDF text extraction
next-themes@^0.4.6          # Light/dark mode
@vercel/functions@^3.4.3    # waitUntil for background pipeline
lucide-react@^0.468.0       # Icons
